import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, GitBranch, Plus } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import ImageUpload from "@/components/media/ImageUpload";
import { getGovernanceLabel, getDefaultGovernanceUnitType, GOVERNANCE_ENTITY_TYPES, GOVERNANCE_UNIT_TYPES, GOVERNANCE_STATUS_OPTIONS } from "@/utils/governance";
import { supabase } from "@/lib/supabase/client";

function Field({ label, children }) { return <label className="block space-y-1.5"><span className="text-xs font-medium text-muted-foreground">{label}</span>{children}</label>; }

export default function GovernanceRelationDialog({ open, onOpenChange, mode = "add-child", sourceEntity, candidates = [], categories = [], onCompleted }) {
  const [step, setStep] = useState("choose"); const [existingId, setExistingId] = useState(""); const [name, setName] = useState(""); const [entityType, setEntityType] = useState("unit"); const [unitType, setUnitType] = useState("unit"); const [categoryId, setCategoryId] = useState(""); const [imageUrl, setImageUrl] = useState(null); const [saving, setSaving] = useState(false); const [validFrom, setValidFrom] = useState(new Date().toISOString().slice(0, 10)); const [status, setStatus] = useState("active"); const [validTo, setValidTo] = useState("");
  useEffect(() => { if (!open) { setStep("choose"); setExistingId(""); setName(""); setEntityType("unit"); setUnitType("unit"); setCategoryId(""); setImageUrl(null); setSaving(false); setValidFrom(new Date().toISOString().slice(0, 10)); setStatus("active"); setValidTo(""); } }, [open]);
  useEffect(() => setUnitType(getDefaultGovernanceUnitType(entityType)), [entityType]);
  const filtered = useMemo(() => candidates.filter((item) => item.id !== sourceEntity?.id), [candidates, sourceEntity]);
  const title = mode === "add-child" ? "Add child" : mode === "add-parent" ? "Add parent" : "Change parent";
  const save = async () => {
    if (!sourceEntity?.id) return;
    try {
      setSaving(true);
      if (mode === "change-parent") { const result = await supabase.rpc("set_governance_parent", { p_child_id: sourceEntity.id, p_parent_id: existingId === "none" ? null : existingId || null }); if (!result || result.error) throw result?.error || new Error("Unable to change parent"); toast.success(existingId === "none" ? "Entity is now independent" : "Parent updated"); }
      else if (step === "choose") { setStep("existing"); return; }
      else if (step === "existing") { if (!existingId) return toast.error("Choose an entity"); const result = await supabase.rpc("set_governance_parent", { p_child_id: mode === "add-child" ? existingId : sourceEntity.id, p_parent_id: mode === "add-child" ? sourceEntity.id : existingId }); if (!result || result.error) throw result?.error || new Error("Unable to update governance relationship"); toast.success("Governance relationship updated"); }
      else { if (!name.trim()) return toast.error("Name is required"); if (!validFrom) return toast.error("Valid from is required"); if (["inactive", "deprecated"].includes(status) && !validTo) return toast.error("Add Valid to when the entity becomes inactive"); if (validTo && validTo < validFrom) return toast.error("Valid to cannot be earlier than valid from"); const rpc = mode === "add-parent" ? "add_governance_parent" : "add_governance_child"; const payload = { p_name: name.trim(), p_entity_type: entityType, p_unit_type: unitType, p_status: status, p_valid_from: `${validFrom}T00:00:00Z`, p_valid_to: validTo ? `${validTo}T23:59:59.999Z` : null, p_category_id: categoryId || null, p_image_url: imageUrl || null }; payload[mode === "add-parent" ? "p_child_id" : "p_parent_id"] = sourceEntity.id; const result = await supabase.rpc(rpc, payload); if (!result || result.error) throw result?.error || new Error("Unable to create governance entity"); toast.success(mode === "add-parent" ? "Parent added" : "Child added"); }
      await onCompleted?.(); onOpenChange?.(false);
    } catch (error) { toast.error(error?.message || "Unable to update governance relationship"); } finally { setSaving(false); }
  };
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-lg"><DialogHeader><DialogTitle className="flex items-center gap-2"><GitBranch className="h-4 w-4" />{title}</DialogTitle></DialogHeader><div className="space-y-4 py-2"><div className="rounded-lg border bg-muted/30 p-3 text-sm"><span className="font-medium">{getGovernanceLabel(sourceEntity)}</span><span className="mx-1 text-muted-foreground">·</span><span className="text-muted-foreground">{mode === "add-child" ? "parent of" : mode === "add-parent" ? "child of" : "new parent for"}</span></div>{mode === "change-parent" ? <Field label="Parent"><Select value={existingId} onValueChange={setExistingId}><SelectTrigger><SelectValue placeholder="Choose a parent" /></SelectTrigger><SelectContent className="max-h-72"><SelectItem value="none">No parent — make independent</SelectItem>{filtered.map((item) => <SelectItem key={item.id} value={item.id}>{getGovernanceLabel(item)}</SelectItem>)}</SelectContent></Select></Field> : step === "choose" ? <div className="grid gap-3 sm:grid-cols-2"><Button type="button" variant="outline" className="h-auto justify-start p-4 text-left" onClick={() => setStep("existing")}><div><div className="font-medium">Use an existing entity</div><div className="mt-1 text-xs text-muted-foreground">Connect a record already in Governance.</div></div></Button><Button type="button" variant="outline" className="h-auto justify-start p-4 text-left" onClick={() => setStep("create")}><div><div className="font-medium">Create a new entity</div><div className="mt-1 text-xs text-muted-foreground">Add a new record here.</div></div></Button></div> : step === "existing" ? <Field label={mode === "add-child" ? "Child entity" : "Parent entity"}><Select value={existingId} onValueChange={setExistingId}><SelectTrigger><SelectValue placeholder="Choose an entity" /></SelectTrigger><SelectContent className="max-h-72">{filtered.map((item) => <SelectItem key={item.id} value={item.id}>{getGovernanceLabel(item)}</SelectItem>)}</SelectContent></Select></Field> : <div className="space-y-4"><Field label="Name"><Input value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. Western Railway" autoFocus /></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="Entity type"><Select value={entityType} onValueChange={setEntityType}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{GOVERNANCE_ENTITY_TYPES.map((type) => <SelectItem key={type} value={type}>{type.replace(/_/g, " ").replace(/\b\w/g, (char) => char.toUpperCase())}</SelectItem>)}</SelectContent></Select></Field><Field label="Structural role"><Select value={unitType} onValueChange={setUnitType}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{GOVERNANCE_UNIT_TYPES.map((type) => <SelectItem key={type} value={type}>{type.replace(/_/g, " ").replace(/\b\w/g, (char) => char.toUpperCase())}</SelectItem>)}</SelectContent></Select></div></div><Field label="Category"><Select value={categoryId || "none"} onValueChange={(value) => setCategoryId(value === "none" ? "" : value)}><SelectTrigger><SelectValue placeholder="Choose a category" /></SelectTrigger><SelectContent className="max-h-72"><SelectItem value="none">No category</SelectItem>{categories.map((item) => <SelectItem key={item.id} value={item.id}>{item.name}</SelectItem>)}</SelectContent></Select></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="Valid from"><Input type="date" value={validFrom} onChange={(event) => setValidFrom(event.target.value)} /></Field><Field label="Valid to"><Input type="date" value={validTo} onChange={(event) => setValidTo(event.target.value)} /></Field></div><Field label="Status"><Select value={status} onValueChange={setStatus}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{GOVERNANCE_STATUS_OPTIONS.map(([value, text]) => <SelectItem key={value} value={value}>{text}</SelectItem>)}</SelectContent></Select></Field><p className="text-xs text-muted-foreground">{status === "active" ? "Leave Valid to empty while this entity remains active." : "Enter the date this entity closed or was retired."}</p><ImageUpload bucket="governance" path={`governance/draft-${sourceEntity?.id || "entity"}/logo`} value={imageUrl} onChange={(value) => setImageUrl(value || null)} label="Logo" helperText="PNG, JPG or WebP · up to 5 MB" disabled={saving} /></div>}</div><DialogFooter>{step !== "choose" && mode !== "change-parent" && <Button type="button" variant="ghost" onClick={() => setStep("choose")} disabled={saving}><ArrowLeft className="mr-2 h-4 w-4" />Back</Button>}<Button type="button" onClick={save} disabled={saving}>{saving ? "Saving..." : mode === "add-child" ? <><Plus className="mr-2 h-4 w-4" />Add child</> : mode === "add-parent" ? <><Plus className="mr-2 h-4 w-4" />Add parent</> : "Change parent"}</Button></DialogFooter></DialogContent></Dialog>;
}
